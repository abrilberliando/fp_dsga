# Assets Folder

Letakkan file aset aplikasi di folder ini:

- `logo.png`     — Logo utama aplikasi (tampil di sidebar)
- `favicon.ico`  — Icon browser (opsional)
- `banner.png`   — Banner halaman (opsional)

**Catatan:** File `logo.png` akan otomatis dideteksi oleh `app.py`.
